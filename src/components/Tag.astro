---
import BookmarkIcon from "./icons/BookmarkIcon.astro";

const { tag, color } = Astro.props;
---

<h3 class=`flex flex-row gap-2 rounded-full py-2 px-6 text-white text-2xl sm:text-lg items-center`
    style={{ backgroundColor: color }}>
    <BookmarkIcon />
    {tag}
</h3>